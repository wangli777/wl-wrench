server:
  port: 8091

# 扳手通用组件配置
wl:
  wrench:
    config:
      system: test-system
      register:
        host: 127.0.0.1
        port: 16379
#        password:

# 日志
logging:
  level:
    root: info
  config: classpath:logback-spring.xml